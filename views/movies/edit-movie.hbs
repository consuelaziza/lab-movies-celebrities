<h2>Edit {{movie.title}}</h2>

<form method="POST" action="/movies/{{movie._id}}">
    <div class="row gx-3 gy-2 align-items-center" aria-label="Basic outlined example">
        <label  class="visually-hidden">Title</label>
        <input name="title" type="text" class="form-control" value="{{movie.title}}">
    </div>
    <div class="row gx-3 gy-2 align-items-center" aria-label="Basic outlined example">
        <label for="genre" class="visually-hidden">Genre</label>
        <input name="genre" type="text" class="form-control" value="{{movie.genre}}">
    </div>
    <div class="row gx-3 gy-2 align-items-center" aria-label="Basic outlined example">
        <label for="plot" class="visually-hidden">Plot</label>
        <input name="plot" type="text" class="form-control" value="{{movie.plot}}">
    </div>
    <div class="row gx-3 gy-2 align-items-center" aria-label="Basic outlined example">
<select name="cast" id="cast" multiple>
{{#each celebrities}}
<option value="{{this._id}}">{{this.name}}</option>
{{/each}}
</select>
    </div>
    
    <button type="submit" class="btn btn-outline-success">Submit</button>
</form>